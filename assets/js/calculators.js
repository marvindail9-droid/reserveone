function money(n){return '$'+Number(n||0).toLocaleString(undefined,{maximumFractionDigits:0})}
function calcLoan(prefix){const p=+document.getElementById(prefix+'-amount').value||0;const rate=(+document.getElementById(prefix+'-rate').value||0)/100/12;const yrs=+document.getElementById(prefix+'-years').value||0;const n=yrs*12;let pay=rate?p*rate/(1-Math.pow(1+rate,-n)):p/n;document.getElementById(prefix+'-result').innerHTML='Estimated monthly payment: <strong>'+money(pay)+'</strong><br><span class="muted">Estimate excludes taxes, insurance, fees and maintenance.</span>'}
function calcNet(){let assets=+document.getElementById('assets').value||0;let debts=+document.getElementById('debts').value||0;document.getElementById('net-result').innerHTML='Estimated net worth: <strong>'+money(assets-debts)+'</strong>'}
